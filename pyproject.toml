[project]
name = "secondworld"
version = "0.1.0"
description = "Multi-agent novel drafting CLI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pyyaml",
  "requests",
  "python-dotenv",
]

[dependency-groups]
dev = [
  "pytest",
]
